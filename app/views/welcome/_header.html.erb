<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header" id="idnavcontainer">

    	<a class="navbar-brand" href="#">
        	<%= image_tag('logo.png', size: '44x25') %>
      	</a>
        <!-- TODO, fix below link to link back to home page -->
    	<span class="navbar-link navbar-brand"><%= link_to 'Mappr', root_path %></span>

    	<% if user_signed_in? %>
    		<p class="navbar-text navbar-right"> Hello, <%= link_to(current_user.email , user_path(current_user)) %> </p>
    		<p class="navbar-text navbar-right"> <%= link_to 'Logout', destroy_user_session_path, method: :delete %> </p>
    	<% else %>
    		<p class="navbar-text navbar-right"> <%= link_to 'Sign In', new_user_session_path %> </p>
    	<% end %>
    </div>
  </div>
</nav>
