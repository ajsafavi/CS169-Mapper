<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header" id="idnavcontainer">

    	<a class="navbar-brand" href="#">
        	<%= image_tag('logo.jpg', size: '25x25') %>
      	</a>

    	<a class="navbar-brand" href="#">Mappr</a>

    	<% if user_signed_in? %>
    		<p class="navbar-text navbar-right"> Hello, <%= current_user.email %> </p>
    		<p class="navbar-text navbar-right"> <%= link_to 'Logout', destroy_user_session_path, method: :delete %> </p>
    	<% else %>
    		<p class="navbar-text navbar-right"> <%= link_to 'Sign In', new_user_session_path %> </p>
    	<% end %>
    </div>
  </div>
</nav>
