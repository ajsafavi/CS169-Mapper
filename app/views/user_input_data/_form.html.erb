<%= form_for(@user_input_datum) do |f| %>
  <% if @user_input_datum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_input_datum.errors.count, "error") %> prohibited this user_input_datum from being saved:</h2>

      <ul>
      <% @user_input_datum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
