var AddDataUI = (function () {

	// varButton = $( "#varButton" )
	// locButton = $( "#locButton" )
	// weightButton = $( "#weightButton" )

	// // varButton.addEventListener("click", varClick);
	// // locButton.addEventListener("click", locClick);
	// // weightButton.addEventListener("click", weightClick);

	function varClick() {
	    debugger
	}



	return {
    

  };
})();

